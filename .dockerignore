# 给 docker build 上下文减负，避免把开发期产物 / 凭证带进镜像

# Node
node_modules
npm-debug.log*

# Next.js
.next
out
build

# Prisma 本地 SQLite & 测试痕迹
prisma/dev.db
prisma/dev.db-journal
prisma/migrations/dev.db*

# 环境变量与凭证
.env
.env.*
!.env.example
cookies.txt
*.cookies

# 编辑器 / OS
.DS_Store
.vscode
.idea

# 构建期不需要
docs
*.tsbuildinfo
.git
.gitignore
.next
.vercel

# Docker 自身（避免循环复制）
Dockerfile
docker-compose.yml
.dockerignore
